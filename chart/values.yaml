# 
---
# Default values for tetris-web.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

nameOverride: ""
fullnameOverride: ""

image:
  repository: ghcr.io/davidecui/tetris-03060045
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 31190

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

ingress:
  enabled: false
  className: ""
  hosts:
    - host: tetris.local
      paths:
        - path: /
          pathType: Prefix

app:
  healthPath: /health
